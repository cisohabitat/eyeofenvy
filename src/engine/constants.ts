// Shared world-space tuning constants.

/** Side length of one grid cell in world units. */
export const CELL_SIZE = 3;

/** Height of dungeon walls / ceiling. */
export const WALL_HEIGHT = 3.5;

/** Camera (party eye) height above the floor. */
export const EYE_HEIGHT = 1.6;

/** Duration of a single move/turn tween, in milliseconds. */
export const STEP_MS = 220;
